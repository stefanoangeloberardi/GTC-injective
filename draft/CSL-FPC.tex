% !TEX root = CSL-main.tex
\subsection{Composition of bi-partite Graphs and Size Change Termination}

Binary relations compose: if $R \subseteq X \times Y$ and $R' \subseteq Y \times Z$ then $RR' \subseteq X \times Z$ is such that
$xRR'z$ if and only if there is $y \in Y$ s.t. $xRy$ and $yR'z$, which we abbreviate by $xRyR'z$. Relation compositions are associative 
and have the identities $I_X = \Set{(x,x) \mid x \in X}$ as units. We can extend composition
to bi-partite relations in a way compatible with the information weakly decreasing/strictly
decreasing associated to the partition. 
In the next definition, we say that an arrow in the composition $(S, P) \comp (S', P')$ belongs 
to the subset of stationary arrows if it can be obtained by two stationary
arrows, and only in this way: 
and that it belongs to the subset of progressing arrow if it can also be obtained otherwise,
by at least one composition including at least one progressing arrow.


\begin{definition}[Composition of bi-partite relations]\label{def:bi-partiteRelations}
Let $(S,P)$, $(S',P')$ be bipartite relations over $X$; then we define their {\em composition} $(S, P) \comp (S', P') = (S'' \setminus P'',\; P'')$
where
\[S'' = S S' \qquad \mbox{and} \qquad P'' = (S P') \cup (P S') \cup (P P') \]
We call $|(S,P)| = S \cup P$ the relation associated to $(S,P)$.
\end{definition}

We will prove that the case all relations are injective the definition
can be replaced with $(S, P) \comp (S', P') = (S'', \; P'')$
The composition of bi-partite relations is bi-partite by construction; also, it is associative
with identity the pair $(I_X, \emptyset)$.
We observe that composition is compatible with the operation 
$|\cdot|$ and ordinary composition of binary relations: 
%let's write $|(S,P)| = S \cup P$, then
\begin{align}
\label{eq:unionComp}
|(S,P) \comp (S',P')| = |(S'' \setminus P'', P'')| &= (S''\setminus P'') \cup P''  
= S'' \cup P''  \nonumber \\ = SS' \cup  (S P') \cup (P S') \cup (P P')
& = (S \cup P) (S' \cup P') = |(S,P)| |(S',P')|
\end{align}
%We shall be interested in (finite) sets of bi-partite relations $\biRel = \Set{(S_1,P_1),...,(S_k,P_k)}$ over the same set $X$.
Let $\biRel^*$ be the closure under composition $\comp$ of a set $\biRel$ of bi-partite relations. Notice that the set of bi-partite relations over the finite set $X$
is a subset of $\wp(X^2) \times \wp(X^2)$ which is finite, hence $\biRel^*$ is finite.
The role of a transition graph $\Graph$ with labels in $\biRel$ is to reduce the number of
 compositions of bipartite relations in $\biRel^*$ which are accepted. We formalize this notion
by adapting the usual definition of transitive closure of a graph.

\begin{definition}[Transitive closure of a transition graph]\label{def:transitiveClosure}
We say that  the graph $\Graph^* = (V, \biRel^*, E^*)$ is the {\em transitive closure} of $\Graph = (V, \biRel, E)$ if
$E^* \subseteq V \times \biRel^* \times V$ is the smallest set such that $E \subseteq E^*$ and
\[ (u, (S,P), v), (v, (S',P'), w) \in E^* \Then (u, (S,P) \comp (S',P'), w) \in E^*\]
If $(u, (S,P) \comp (S',P'), w) \in E^*$ for some vertexes $u, w$ then we say that $(S,P)$ and $(S',P')$ are {\em composable} in $\Graph^*$; we say
that $(S,P)$ is {\em self-composable} in $\Graph^*$ if $(v, (S,P), v) \in E^*$ for some vertex $v$.
\end{definition}

In words, $\Graph^*$ is the transitive closure of the graph $\Graph$ with edges labeled by the composition of the labels of composable edges in $\Graph$. 
We could say that it is essentially a category, but for the lack of identities.

Let $\Graph^* = (V, \biRel^*, E^*)$; we abbreviate $(u, (S,P), v) \in E^*$ by %$\edgeG{(S,P)}{u}{v}{\Graph}$ and $(u, (S,P), v) \in E^*$ by 
$\edgeGstar{(S,P)}{u}{v}{\Graph}$. 
%Also, if $e = \edgeG{(S,P)}{u}{v}{\Graph}$ then $\source{e} = u$, $\target{e} = v$, and $\lbl{e} = (S,P)$ are the {\em source}, {\em target}, and {\em label}
%of $e$ respectively.
If $\edgeGstar{(S,P)}{v}{v}{\Graph}$ is self-composable then $\edgeGstar{(S,P)^n}{v}{v}{\Graph}$ for all $n > 0$ by definition of composition over the edges 
of $\Graph^*$, where $(S,P)^n$ is the composition of $(S,P)$ with itself $n$-times.
%, hence the name. % why ???
As before, we omit the subscripts in $\edgestar{(S,P)}{u}{v}$, when $\Graph$ is understood.
In order to state GTC for transition graphs we need the notions of sequence and suffix.

%\begin{definition}[Trace, path and progress]\label{def:TracePath}
%Let $D$ be some initial segment of $\Nat$, namely $D=[1,n]$ for some $n \in \Nat$
%or $D=\Nat$, and 
%$\Graph = (V, \biRel, E)$ a transition graph; then:
%\begin{enumerate}[i)]
%\item a {\em trace} is a map $\tau:D \to X$;
%\item a {\em path} is a map $\ppath:D \to E$ such that for all $i + 1 \in D$, 
%$\target{\ppath(i)} = \source{\ppath(i+1)}$;
%\item a trace $\tau:D \to X$ {\em follows} a path 
%$\ppath: D \to E$ written $\tau \models \ppath$, if 
%	for all $i + 1 \in D$,
%	if $\lbl{\ppath(i)} = (S,P)$ then $\tau(i) (S \cup P) \tau(i + 1)$. 
%If $\tau(i) P \tau(i + 1)$ we say that $i$ is a {\em progress point} of $\tau$ in $\ppath$.
%\item
%$\tau$ is infinitely progressing if and only if $\tau$ has infinitely many progress point.
%\item
%A path $\ppath':D' \rightarrow E$ is a tail of a path $\ppath':D \rightarrow E$ if and only
%if $D'$ is obtained by removing some initial segment of $D$.
%\end{enumerate}
%\end{definition}

%FIN QUI 13:34 14/07/2025

\begin{definition}[Sequence and Inhabited Suffix]
Let $\alpha \leq \omega$ be an ordinal, 
identified with an initial segment of $\Nat$ 
ordered in the natural way, and possibly coinciding with $\Nat$. Let $k \in \alpha$.
\begin{enumerate}
\item
a sequence over some set $A$ with domain $\alpha$ is just a map $s:\alpha \to A$;  

\item
$\alpha\uparrow_k \;=\; \Set{i \in \alpha \mid i \geq k}$ is an inhabited suffix of $\alpha$; 

%\item
%The map $s\uparrow_k$ which is $s$ restricted to $\alpha\uparrow_k$ is a {\em tail} of $s$. 
\end{enumerate}
\end{definition}

We can now restate the notion of trace and the Global Trace Condition for transition graphs.

\begin{definition}[Trace, path and progress]\label{def:TracePath}
Let $\Graph = (V, \biRel, E)$ be a transition graph and $\alpha \leq \omega$, $k \in \alpha$; 
then:
\begin{enumerate}[i)]
\item 
a {\em trace} is a map $\tau:\alpha\uparrow_k \to X$ with domain some inhabited 
suffix of $\alpha$;

\item 
a {\em path} is a map $\ppath:\alpha \to E$ such that if $i + 1 \in \alpha$ then 
$\target{\ppath(i)} = \source{\ppath(i+1)}$;

\item 
a trace $\tau:\alpha\uparrow_k \to X$ {\em follows} a path $\ppath: \alpha \to E$,
written $\tau \models \ppath$, if for all $i,i+1 \in \alpha\uparrow_k$,
if $\lbl{\ppath(i)} = (S,P)$ then $\tau(i) (S \cup P) \tau(i + 1)$.

If furthermore $\tau(i) P \tau(i + 1)$ 
we say that $i$ is a {\em progress point} of $\tau$ in $\ppath$.

\item 
a trace $\tau$ such that $\tau\models \ppath$ 
is infinitely progressing if $\tau$ has infinitely many progress points in $\ppath$.
\end{enumerate}
\end{definition}


\begin{remark}
A trace $\tau = x_k, x_{k+1}, x_{k+2} \cdots$ follows a path $\ppath: v_0 \stackrel{(S_0,P_0)}{\longrightarrow} v_1 \stackrel{(S_1,P_1)}{\longrightarrow} v_2 \cdots$
if for $\tau$, $\ppath$ have domain $\alpha\uparrow_k$, $\alpha$, respectively, 
and $x_i (S_i \cup P_i) x_{i+1}$ for all $i, i+1 \in \alpha\uparrow_k$. 
Hence, for $\tau\models\ppath$ to hold, it suffices that the above relation holds, possibly disregarding a initial segment of $\ppath$ which is a proper subset.
In the particular case that $\alpha < \omega$, namely when both $\tau$ and $\ppath$ are finite, there is no condition about $x_{\alpha - 1}$ w.r.t. $S_{\alpha - 1} \cup P_{\alpha - 1}$, but this is immaterial for our development, 
as for us the only interesting paths and traces are the infinite ones.
\end{remark}

We say that $\tau$ is a trace of $\Graph$ if 
$\tau \models \ppath$ for some path $\ppath$ of $\Graph$.
In our terminology, the path $\ppath$ and the trace $\tau$ correspond to a {\em multipath} $\mathcal{M}$ and a {\em thread} $\textit{th}$ in \cite{Lee1}, respectively.
%We say that a trace $\tau:\alpha \to X$, respectively a path $\ppath:\alpha \to E$ are infinite if 
%$\alpha=\Nat$, finite otherwise.
Both $\Graph$ and $\Graph^*$ are finite, so that an infinite path $\ppath$ exists in $\Graph$ only if it has a cycle, and therefore there exists
some $(S,P) \in \biRel^*$ which is self-composable in $\Graph^*$.

We are eventually in place to define the size-change termination condition for transition graphs, which is the same as \cite[Def. 2.6]{Lee3}, and expresses Global Trace Condition for 
bi-partite graphs.

\begin{definition}[Size-change Termination - SCT]
\label{def:SCT}
The transition graph $\Graph$ satisfies the {\em Size-change Termination} condition 
if for all infinite path $\ppath$ of $\Graph$ there is some infinitely progressing trace $\tau$ 
such that $\tau \models \ppath$.
\end{definition}

%\medskip
In the rest of this section, we relate the progress of a trace $\tau$ w.r.t. $\ppath$ 
to the composition $(S,P)$ of the bi-partite relations labeling the edges in $\ppath$,
and show that progressing is equivalent to having $x, y$ in the image of $\tau$ such that $x$ precedes $y$ in $\tau$ and $x P y$.

\begin{lemma}\label{lem:P-lemma}
Let $(S,P) = (S_1,P_1)\comp (S_2,P_2)$ and $x (S_1 \cup P_1) y (S_2 \cup P_2) z$, then
\begin{enumerate}[i)]

\item
\label{lem:P-lemma-1} $x P_1 y \Then x P z$

\item
\label{lem:P-lemma-2} $y P_2 z \Then x P z$

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% TO BE INSERTED IN THE JOURNAL VERSION IF ANY
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%\item 
%\label{lem:P-lemma-3} 
% If $S_2 \cup P_2$ is injective, then $x S_1 y \And y S_2 z \Then x S z$
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\end{enumerate}
\end{lemma}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% TO BE INSERTED IN THE JOURNAL VERSION IF ANY
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%\begin{proof}
%\begin{enumerate}
%\item
%By definition of composition, we have $P = S_1P_2 \cup P_1S_2 \cup P_1P_2$. 
%Now $x P_1 y$ implies $x (P_1S_2 \cup P_1P_2) z$ and $y P_2 z$ implies 
%$x (S_1P_2 \cup P_1P_2) z$ which establish 
%(\ref{lem:P-lemma-1}) and (\ref{lem:P-lemma-2}). 
%\item
%To show part (\ref{lem:P-lemma-3}),
%assume that $x S_1 y$ and $y S_2 z$ in order to show that $x S z$. We have $x S_1S_2 z$.
%In order to show that $x S z$ we have to show that $\neg (x P z)$, namely that 
%if $x (S_1\cup P_1) y'$ and $y' (S_2 \cup P_2) z$ then $\neg (x P_1 y')$ and $\neg (y' P_2 z)$.
%By injectivity of $S_2 \cup P_2$ from $y (S_2 \cup P_2) z$ and $y' (S_2 \cup P_2) z$
%we deduce that $y = y'$, therefore $x S_1 y'$ and $y' S_2 z$. We conclude that
%$\neg (x P_1 y')$ and $\neg (y' P_2 z)$ from $S_1 \cap P_2 = \emptyset$ and 
%$S_2 \cap P_1= \emptyset$.
%\end{enumerate}
%\end{proof}
%

If $\ppath: v_0 \stackrel{(S_0,P_0)}{\longrightarrow} v_1 \cdots \; v_{b-1} \stackrel{(S_{b-1},P_{b-1})}{\longrightarrow} v_{b}$ is a (finite) path of $\Graph$, for $0 \leq i < j < b$
we abbreviate $(S_{i,j}, P_{i,j}) = \bigcomp_{p = i}^{j} (S_p,P_p) = (S_i,P_i) \comp \cdots \comp (S_j,P_j)$. %The next Proposition states that


\begin{proposition}\label{prop:progresing-and-P}
Let $\ppath: v_0 \stackrel{(S_0,P_0)}{\longrightarrow} v_1 \cdots \; v_{b-1} \stackrel{(S_{b-1},P_{b-1})}{\longrightarrow} v_{b}$ be a finite path of $\Graph$
and $\tau = x_a, \ldots , x_{b-1}$ be a trace such that $\tau \models \ppath$. Then $\tau$ has a progress point in $\ppath$ if and only if
there exists $a \leq h < b$ such that
\[x_h P_{h,b-1} x_b \And \forall h \leq j < b.\; x_j (S_j \cup P_j) x_{j+1}\]
%\begin{enumerate}[i)]
%\item $x_h P_{i_0,b-1} x_b$ %where $(S,P) = (S_{i_0,b-1},P_{i_0,b-1})$
%\item $x_j (S_j \cup P_j) x_{j+1}$ for all $i_0 \leq j < b$
%\end{enumerate}
\end{proposition}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% TO BE INSERTED IN THE JOURNAL VERSION IF ANY
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%
%\begin{proof} To prove the if part, assume that $x_h P x_b$ for some $0 \leq h < b$; then $x_h P_h x_{h+1} (S_{h+1, b}, P_{h+1,b})x_b$ by the
%hypothesis that $\tau\models\ppath$ and the definition of composition.
%
%To prove the only if part, let us assume that $\tau\models\ppath$, hence there is $i_0$ such that $x_j (S_j \cup P_j) x_{j+1}$ for all $i_0 \leq j < b$, and that there is 
%$i_0 \leq h < b$ such that $x_h P_h x_{h+1}$ which is a progress point of $\tau$ in $\ppath$; w.l.o.g. let us assume that $i_0 = 0$.
%
%We know that $(S, P) = (S',P') \comp (S_h,P_h) \comp (S'',P'')$, where $(S',P')= (S_{0,h-1},P_{0,h-1})$ and 
%$(S'',P'')= (S_{h+1,b-1},P_{h+1,b-1})$, and 
%\[x_0 (S' \cup P') x_h P_h x_{h+1} (S'' \cup P'') x_b \]
%
%Let us abbreviate $(S,P) = (S_{h,b-1},P_{h,b-1})$.
%If $(\hat{S},\hat{P}) = (S',P') \comp (S_h,P_h)$ then by (\ref{lem:P-lemma-2}) of Lemma \ref{lem:P-lemma} we have $x_0 \hat{P} x_{h+1}$; from this and 
%$(S,P) = (\hat{S},\hat{P}) \comp (S'', P'')$, we conclude that $x_0 P x_b$  by (\ref{lem:P-lemma-1}) of the same Lemma, as desired.
%\end{proof}
%% If $\tau \models\ppath$ in $\Graph$
%% and $\ppath$ is the path $\edgeGstar{(S,P)}{u}{v}{\Graph}$ then we say that $\tau$ 
%% progresses in $(S,P)$ if 



%%%%%%%%%%%% Removed part %%%%%%%%%%%%%%%
\if false
\marginpar{The next parts in this section can be removed in the conference version}

\begin{lemma}
\label{lem:SCT}
A transition graph $\Graph$ satisfies the SCT condition if and only if for all self-composable 
$\edgeGstar{(S,P)}{u}{u}{\Graph}$ such that 
$(S,P) \comp (S,P) = (S,P)$ there is some $x \in X$ such that $x P x$.
\end{lemma}

In view of Lemma \ref{lem:SCT}, SCT can be formulated in many equivalent ways, for instance, as Finite Power Condition (FPC):

\begin{definition}[Finite Power Condition - FPC]
\label{def:FPC}
$\Graph$ satisfies the {\em Finite Power Condition} if for all self-composable 
$\edgestar{(S,P)}{u}{u}$ there are some $n > 0 $ and
$x \in X$ such that $x$ progresses to $x$ in $(S,P)^n$.
\end{definition}


To prove the equivalence between SCT and FPC we use a folklore
algebraic property, that all elements of a finite monoid have some positive idempotent power. For $n > 0$, define $(S,P)^n$ as the $n$-times
self-composition of $(S,P)$.

\begin{lemma}[Idempotent Power]
\label{lem:idempotent-power}
For all $(S,P) \in \biRel^*$ there is $n > 0$ such that $(S,P)^n = (S,P)^n \comp (S,P)^n$.
%Let $M$ be a finite monoid and $x \in M$, then for some $n>0$ we have $x^nx^n=x^n$, where $x^n$ is the $n$-times product of $x$ with itself.
\end{lemma}

\begin{proof}
Since $\biRel^*$ is finite, there are only finitely many powers of $(S,P)$, therefore 
for some $a,b>0$ we have $(S,P)^a = (S,P)^{a+b}$. Then for all $c \ge a$ we have $(S,P)^c = (S,P)^{c+b}$,
hence $(S,P)^c = (S,P)^{c+db}$ for all $d>0$. Take the first $d>0$ such that $db \ge a$: then 
$(S,P)^{db} = (S,P)^{db+db} = (S,P)^{db} \comp (S,P)^{db}$. We take $n=db$.
\end{proof}

%\begin{remark}\label{rem:idempotent-power}
%Now $\biRel^*$, which is finite, but neither $\biRel^*$ nor the set of bi-partite relations that are composable in $\Graph$ is a monoid because of the lack of identities, hence of the unit w.r.t. the composition $\comp$. 
%Nonetheless, the proof of the above proposition does not rely on the existence of the unit in $M$, only on the associativity of its product, hence it applies
%to $(\biRel^*, \comp)$.
%\end{remark}

\begin{proposition}[FPC and SCT are equivalent]
\label{proposition-SCT}
SCT $\Leftrightarrow$ FPC
\end{proposition}

\begin{proof}
%\begin{description}
%\item
Assume SCT, $\edgeGstar{(S,P)}{u}{u}{\Graph}$.
By Lemma \ref{lem:idempotent-power} and finiteness of the bi-partite graph $\Graph$
there is some $n > 0$ such that $(S,P)^n \comp (S,P)^n=(S,P)^n$. By SCT,
there is some $x \in X$ such that $x$ progresses to $x$ in $(S,P)^n$.
%\item

Assume FPC, $\edgeGstar{(S,P)}{u}{u}{\Graph}$ and 
$(S,P) \comp (S,P)=(S,P)$ in order to prove that 
there is some $x \in X$ such that $x$ progresses to $x$ in $(S,P)$.
By FPC, there are some $n > 0 $ and $x \in X$ such that $x$ progresses to $x$ in 
$(S,P)^n$. By $(S,P)\comp (S,P)=(S,P)$ we deduce that 
$(S,P)^n=(S,P)$, hence $x$ progresses to $x$ in $(S,P)$.
%\end{description}
\end{proof}

\fi
%%%%%%%% EOF %%%%%%%%%
