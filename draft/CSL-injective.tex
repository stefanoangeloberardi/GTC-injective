% !TEX root = CSL-main.tex
\section{Global Trace Condition and Injective Relations}
\label{sec:injective}


In this section we define the global trace condition (GTC), a validity criteria used to verify the soundness of circular proofs, using transition graphs with bi-partite relations. As our primary examples, we consider the cyclic proof systems $\CLKIDomega$ and $\CLJIDomega$, and show how the GTC for both systems can be expressed in our framework. Our approach is motivated by a well-known relationship in the folklore and formally stated by Ikebuchi in \cite{Mirai}, between the GTC and size-change termination (SCT). Specifically, it consists of extracting a transition graph with bi-partite relations, of a cyclic proof in either $\CLKIDomega$ or $\CLJIDomega$. Moreover, we argue that for these particular proof systems, the obtained bi-partite relations are always injective. This condition will play a key role in establishing our main result later in the paper. We begin by introducing cyclic proofs in both $\CLKIDomega$ and $\CLJIDomega$.

\subsection{Cyclic Proof Systems}
\label{subsec:cyclicProof}

$\CLKIDomega$ and $\CLJIDomega$ are both first order logics, hence we will consider a first-order signature $\Sigma$, which contain denumerable many constant, function and predicate symbols. Predicate symbols are divided between finitely many \emph{inductive predicates}, as in Martin-Löfs \emph{productions} \cite{MartinLof}, and denumerable many \emph{ordinary predicates}, defined as usual predicates. Terms, formulas and $\Sigma$-structures are defined as usually for first order logic. In the case of $\CLKIDomega$, sequents have the form $\Gamma \vdash \Delta$, while in $\CLJIDomega$, they are defined as $\Gamma \vdash \phi$, where $\Gamma$ and $\Delta$ are sets of formulas, and $\phi$ is a single formula. To formally define inductive predicates, we introduce the notion of \emph{inductive definition set}:


\begin{definition}[Inductive definition set]
    \label{defIndSet}
    An inductive definition set $\Phi$ is a finite set of \emph{productions} of the form:

    \begin{center}
        \centering
        \begin{prooftree}
            \hypo{Q_1\mathbf{u_1} \ldots Q_h\mathbf{u_h}}
            \hypo{P_1\mathbf{t_1} \ldots P_m\mathbf{t_m}}
            \infer2[]{P\mathbf{t}}
        \end{prooftree}
    \end{center}

    Where $Q_1, \ldots, Q_h$ are ordinary predicates symbols, $P_1, \ldots, P_m$ are inductive predicate symbols, and $\mathbf{u_1}, \ldots, \mathbf{u_h}$, $\mathbf{t_1}, \ldots, \mathbf{t_m}$, $\mathbf{u_1}, \ldots, \mathbf{t}$ are tuples of terms.
\end{definition}

\begin{figure}
    \centering
    \includegraphics[scale = 0.5]{example-image}
    \caption{Rules of LK.}
    \label{LKrules}
\end{figure}


\begin{example}
We define the inductive definition set $\Phi_{NEO}$, using the following productions rules:

\begin{center}
    \centering
    \begin{minipage}{0.10\textwidth}
        \centering
        \begin{prooftree}
            \hypo{}
            \infer1[]{N0}
        \end{prooftree}
    \end{minipage}
    \begin{minipage}{0.10\textwidth}
        \centering
        \begin{prooftree}
            \hypo{Nx}
            \infer1[]{Nsx}
        \end{prooftree}
    \end{minipage}
    \begin{minipage}{0.10\textwidth}
        \centering
        \begin{prooftree}
            \hypo{}
            \infer1[]{E0}
        \end{prooftree}
    \end{minipage}
    \begin{minipage}{0.15\textwidth}
        \centering
        \begin{prooftree}
            \hypo{Ox}
            \infer1[]{Esx}
        \end{prooftree}
    \end{minipage}
    \begin{minipage}{0.12\textwidth}
        \centering
        \begin{prooftree}
            \hypo{Ex}
            \infer1[]{Osx}
        \end{prooftree}
    \end{minipage}
\end{center}

Where $0$ is a constant representing the number zero and $s$ is a unary function representing the successor function. The predicate $N$ is used to inductively represent the natural numbers, while $E$ and $O$ are \emph{mutually defined} inductive predicates defining even and odd numbers respectively.
\end{example}

We define the proof system for $\CLKIDomega$ as the extension of the classical logic LK, defined in Figure \ref{LKrules}, with additional rules used to handle inductive predicates on the right and left part of the sequent. These rules for inductive predicates are defined as follows:

\begin{itemize}
    \item Let $P$ be an inductive predicate, and let $\{\Phi_1, \ldots, \Phi_n\}$ be the set of productions in an inductive definition set $\Phi$ whose conclusion is $P$. For each production $\Phi_i$, as defined in Definition \ref{defIndSet}), the system $\CLKIDomega$ includes a corresponding right introduction rule, denoted $P\text{R}_i$:

    \begin{center}
        \centering
        \begin{prooftree}
            \hypo{\Gamma \vdash Q_1\mathbf{u_1}, \Delta \ldots \Gamma \vdash Q_h\mathbf{u_h}, \Delta}
            \hypo{\Gamma \vdash P_1\mathbf{t_1}, \Delta \ldots \Gamma \vdash P_m\mathbf{t_m}, \Delta}
            \infer2[($P\text{R}_i$)]{\Gamma \vdash P\mathbf{t}, \Delta}
        \end{prooftree}
    \end{center}


    \item For each inductive predicate $P$ there is a left introduction rule, denoted \emph{case split rule}:

    \begin{center}
        \centering
        \begin{prooftree}
            \hypo{\text{case distinctions}}
            \infer1[(Case $P$)]{\Gamma, P\mathbf{u} \vdash \Delta}
        \end{prooftree}
    \end{center}

    Where for each production having conclusion $P$, as in Definition \ref{defIndSet}, there is a corresponding case distinction of the form $\Gamma, \mathbf{u} = \mathbf{t}, Q_1\mathbf{u_1}, \ldots , Q_h\mathbf{u_h}, P_1\mathbf{t_1}, \ldots, P_m\mathbf{t_m} \vdash \Delta$ subject to the restriction that any free variable in $\mathbf{u_1},...,\mathbf{u_h},\mathbf{t_1},...,\mathbf{t_m}$, or $\mathbf{t}$, does not occur in $\Gamma, \Delta$ and $\mathbf{u}$.
\end{itemize}

The proof system for $\CLJIDomega$ is naturally obtained by restricting the rules for inductive predicates over intuitionistic sequents, and replacing LK with its intuitionistic version, named LJ, which is presented in \cite{Berardi}.

\begin{example}
For the previously defined inductive predicates $N$, $E$ and $O$, we can associate the following rules:    

\begin{center}
    \centering
    \begin{minipage}{0.2\textwidth}
        \adjustbox{scale=0.8}{
        \begin{prooftree}
            \hypo{}
            \infer1[($E\text{R}_1$)]{\Gamma \vdash E0, \Delta}
        \end{prooftree}
        }
    \end{minipage}
    \hfill
    \begin{minipage}{0.2\textwidth}
        \adjustbox{scale=0.8}{
        \begin{prooftree}
            \hypo{\Gamma \vdash Ox, \Delta}
            \infer1[($E\text{R}_2$)]{\Gamma \vdash Esx, \Delta}
        \end{prooftree}
        }
    \end{minipage}
    \hfill
    \begin{minipage}{0.2\textwidth}
        \adjustbox{scale=0.8}{
        \begin{prooftree}
            \hypo{\Gamma \vdash Ex, \Delta}
            \infer1[($O\text{R}_1$)]{\Gamma \vdash Osx, \Delta}
        \end{prooftree}
        }
    \end{minipage}
    \hfill
    \begin{minipage}{0.35\textwidth}
        \adjustbox{scale=0.8}{
        \begin{prooftree}
            \hypo{\Gamma, x = 0 \vdash \Delta}
            \hypo{\Gamma, x = sz, Nz \vdash \Delta}
            \infer2[(Case $N$)]{\Gamma, Nx \vdash \Delta}
        \end{prooftree}
        }
    \end{minipage}

\end{center}
\end{example}

\emph{Cyclic pre-proofs} are used to formally represent the graph structure of a derivation in $\CLKIDomega$ and $\CLJIDomega$.

\begin{definition}[Cyclic pre-proof] A $\CLKIDomega$ (resp. $\CLJIDomega$) pre-proof of sequent $\Gamma \vdash \Delta$ (resp. $\Gamma \vdash \phi$), is a tuple $\Pi = (\mathcal{D}, \mathcal{F})$, where:

    \begin{itemize}
        \item $\mathcal{D} = (V, s, r, p)$ is a derivation tree where $V$ is a finite set of vertices, $s: V \rightarrow Seqs$ is a total function labelling vertices with sequents in $\CLKIDomega$ (resp. $\CLJIDomega$), $r: V \rightarrow Rules$ is a partial function labelling vertices with inference rules in $\CLKIDomega$ (resp. $\CLJIDomega$), $p: \mathbb{N} \times V \rightarrow V$ is a partial function that maps a number $n$ and a vertex $v$, with a vertex $v'$, such that $v'$ is labeled by the $n^{\text{th}}$ premise of the rule $r(v)$ over the sequent $s(v)$, and for every $v \in V$, 
            \begin{prooftree}
                \hypo{\vdash s(p_1(v)) \quad \dots \quad \vdash s(p_m(v))}
                \infer1[$(r(v))$]{\vdash s(v)}
            \end{prooftree} 
        is an instance of the rule $r(v)$ in $\CLKIDomega$ (resp. $\CLJIDomega$), with $m$ premises. A vertex $B \in V$ such that $r(B)$ is undefined is called a bud node. The set of bud nodes of a derivation tree $\mathcal{D}$, is written as $Bud(\mathcal{D})$.

        \item $\mathcal{F}: V \rightarrow V$ is a partial function, that maps each one of the bud nodes in $Bud(\mathcal{D})$ to an internal node in the derivation tree, called a companion, satisfying that if $B \in Bud(\mathcal{D})$ and $\mathcal{F}(B) = C$, then $r(C)$ is defined and $s(B) = s(C)$.
    \end{itemize}

Moreover, the graph of $\Pi$, written as $G_{\Pi}$, is the graph obtained after adding the edges induced by $\mathcal{F}$ and deleting the bud vertices. Specifically, $G_{\Pi} = (V', s, r, p')$ where, $V' = V \setminus Bud(\mathcal{D})$ and $p'$ is defined for any $j$ as: $p'_j(v) = \mathcal{F}(p_j(v))$ if $p_j(v) \in Bud(\mathcal{D})$, or $p'_j(v) = p_j(v)$ otherwise.
\end{definition}

\begin{example}

\begin{figure}
\centering

\adjustbox{scale=0.8}{
\begin{prooftree}
	\hypo{}
	\infer1[($E\text{R}_1$)]{\vdash E0, O0}

	\hypo{Nx \vdash Ox, Ex \ (\dag)}
	\infer1[(Subst)]{Ny \vdash Oy, Ey}
	\infer1[($O\text{R}_1$)]{Ny \vdash Oy, Osy}
	\infer1[($E\text{R}_2$)]{Ny \vdash Esy, Osy}
	\infer1[($=$L)]{x = sy, Ny \vdash Ex, Ox}
	\infer2[(Case $N$)]{Nx \vdash Ex, Ox \ (\dag)}
	\infer1[($\lor$R)]{Nx \vdash Ex \lor Ox}
\end{prooftree}
}

\caption{$\CLKIDomega$ cyclic pre-proof of the sequent $Nx \vdash Ex \lor Ox$. The function $\mathcal{F}$ is represented by labeling each bud and its corresponding companion node with the $(\dag)$ symbol.}
\label{exampleCircProof}
\end{figure}

Figure \ref{exampleCircProof} is an example of a $\CLKIDomega$ cyclic pre-proof, taken from \cite{Brotherston1}.

\end{example}

The GTC is used as a criterion to verify the soundness of cyclic pre-proofs in $\CLKIDomega$ and $\CLJIDomega$. To define it, is necessary to formalize both the notions of \emph{trace} and \emph{progress point}.


\begin{definition}[Trace and Progress Point]
    Let $\Pi$ be a $\CLKIDomega$ (resp. $\CLJIDomega$) pre-proof and let $(v_i)$ be a path in $G_{\Pi}$. A trace over $(v_i)$ is a sequence $(\uptau_i)$ such that, for all $i$:

    \begin{itemize}
          \item $\uptau_i = P_i\mathbf{t_i} \in \Gamma_i$, where $P_i$ as and inductive predicate and $s(v_i) = \Gamma_i \vdash \Delta_i$ (resp. $s(v_i) = \Gamma_i \vdash \phi_i$).

          \item If $r(v_i)$ is (Subst) then $\uptau_i = \uptau_{i+1}[\theta]$, where $\theta$ is the substitution associated with the rule instance.

          \item If $r(v_i)$ is (=L2) with active formula $t = u$ then there exists a formula $F$ and variables $x$ and $y$ such that $\uptau_i = F[t / x, u/y]$ and $\uptau_{i+1} = F[u / x, t/y]$

          \item If $r(v_i)$ is some case split rule (Case $P_k$) then either $\uptau_{i+1} = \uptau_i$, or $\uptau_i$ is the active formula of the rule instance and $\uptau_{i+1}$ is a case-descendant of $\uptau_i$. In the latter case, $i$ is said to be a progress point of the trace.

          \item If $r(v_i)$ is no (Subst),(=L) or a casesplit rule, the $\uptau_{i+1} = \uptau_i$
    \end{itemize}  

\end{definition}

\begin{definition}[Global Trace Condition - Pre-Proofs]
    A $\CLKIDomega$ (resp. $\CLJIDomega$) pre-proof $\Pi$ is said to be a $\CLKIDomega$ (resp. $\CLJIDomega$) \emph{proof}, if for every infinite path in $G_{\Pi}$, there is an infinitely progressing trace following some tail of the path.
\end{definition}

\begin{example}
    Consider the cyclic pre-proof in Figure \ref{exampleCircProof}. The trace $\uptau = (Nx, Ny, Ny, Ny, Ny, Nx)^{\omega}$ is infinitely progressing, since it contains the progressing point $(Nx,Ny)$ given by the (Case $N$) rule. Since this is the only infinite path in the pre-proof, we say that it satisfies the GTC.
\end{example}

%----------------------------------------------------
%----------------------------------------------------
%----------------------------------------------------
%----------------------------------------------------
%----------------------------------------------------

\subsection{Transition Graphs of Cyclic Pre-Proofs}
\label{subsec:GTC}

Given a $\CLKIDomega$ or $\CLJIDomega$ pre-proof $\Pi$, we show how to construct the induced transition graph with bi-partite relations. This construction follows the same method used by Ikebuchi in \cite{Mirai}.


\begin{definition}[Transition Graph of a Pre-Proof]
\label{def:preProofTrans}

Let $\Pi = ((V, s, r, p), \mathcal{F})$ be a $\CLKIDomega$ or $\CLJIDomega$ pre-proof with a graph $G_{\Pi} = (V',s,r,p')$. We note $\mathcal{G}_{\Pi} = (V', \mathcal{R}, E)$ as the transition graph over bi-partite relations induced by $\Pi$, where:
\begin{itemize}
     \item Let $v_1,v_2 \in V'$ such that there exists a $j$ such that $p'_j(v_1) = v_2$. Moreover, let $s(v_1) = \Gamma_1 \vdash \Delta_1$ (resp. $s(v_2) = \Gamma_2 \vdash \Delta_2$) where $\Gamma_1$ (resp. $\Gamma_2$) contains the inductive predicates $P_1\mathbf{t_1}, \ldots, P_n\mathbf{t_n}$ (resp. $Q_1\mathbf{s_1}, \ldots, Q_m\mathbf{s_m}$). We define the bi-partite relation $(S,P)_{v_1,v_2}$ as follows:
        \begin{enumerate}[i)]
            \item The domain and codomain of $(S,P)_{v_1,v_2}$ are $\{P_1\mathbf{t_1}, \ldots, P_n\mathbf{t_n} \}$ and $\{Q_1\mathbf{s_1}, \ldots, Q_m\mathbf{s_m} \}$ respectively.
            \item $(P_k\mathbf{t_k}) S (Q_l\mathbf{s_l})$ if and only if there is a non-progressing trace $\uptau = (P_k\mathbf{t_k},Q_l\mathbf{s_l})$, for any $k$ and $l$.
            \item $(P_k\mathbf{t_k}) P (Q_l\mathbf{s_l})$ if and only if there is a progressing trace $\uptau = (P_k\mathbf{t_k},Q_l\mathbf{s_l})$, for any $k$ and $l$.
        \end{enumerate}
    
    \item For all vertices $v_1,v_2 \in V'$ such that there exists a $j$ such that $p'_j(v_1) = v_2$, then $(S,P)_{v_1,v_2} \in \mathcal{R}$ and $\edge{(S,P)_{v_1,v_2}}{v_1}{v_2} \in E$.
 
 \end{itemize} 

\end{definition}

The next step is to provide a formal definition of the GTC in the context of transition graphs with bi-partite relations, and show its equivalence to the GTC for pre-proofs in $\CLKIDomega$ and $\CLJIDomega$. 

\begin{definition}[Trace, path and progress]\label{def:TracePath}
Let $\alpha \leq \omega$ be an ordinal, and $\Graph = (V, \biRel, E)$ a transition graph; then:
\begin{enumerate}[i)]
\item a {\em trace} is a map $\tau:\alpha \to X$;
\item a {\em path} is a map $\ppath:\alpha \to E$ such that for all $i + 1 < \alpha$, $\target{\ppath(i)} = \source{\ppath(i+1)}$;
\item a trace $\tau$ is {\em compatible} with a path $\ppath$ (with the same domain $\alpha$), written $\tau \models \ppath$, if 
	there is $i_0 < \alpha$ such that for all $i_0 \leq i < i+1 < \alpha$,
	if $\lbl{\ppath(i)} = (S,P)$ then $\tau(i) (S \cup P) \tau(i + 1)$, and if $\tau(i) P \tau(i + 1)$ then $i$
	is a {\em progress point} of $\tau$ in $\ppath$.
\end{enumerate}
\end{definition}

A trace $\tau:\alpha \to X$, respectively a path $\ppath:\alpha \to E$ are infinite if $\alpha = \omega$, finite otherwise.
We say that $\tau$ is a trace of $\Graph$ if $\tau \models \ppath$ for some path $\ppath$ of $\Graph$. 

%If $\tau \models\ppath$ in $\Graph$
%and $\ppath$ is the path $\edgeGstar{(S,P)}{u}{v}{\Graph}$ then we say that $\tau$ progresses in $(S,P)$ if 



\begin{definition}[Global Trace Condition - Transition Graphs \textcolor{red}{(I would suggest calling this SCT instead of GTC, since )}]
\label{def:GTC}
The transition graph $\Graph$ satisfies the {\em Global Trace Condition} if for all infinite path $\ppath$ of $\Graph$ there is an infinite trace $\tau$ such that
$\tau \models \ppath$, which has infinitely many progress points.
\end{definition}

\begin{proposition}
Let $\Pi$ be a $\CLKIDomega$ (resp. $\CLJIDomega$) pre-proof. Then $\Pi$ is a $\CLKIDomega$ (resp. $\CLJIDomega$) proof if and only if $\mathcal{G}_{\Pi}$ satisfies the global trace condition.
\end{proposition}


%\medskip Here we introduce the system $\CLKIDomega$ and its particular case $\CLJIDomega$, and show that our GTC is the same as the correctness condition in \cite{BrotherstonSimpson}.

%Using \cite{Mirai}, we argue that GTC = SCT. 

\subsection{Injective Bi-partite Relations}
\label{subsec:injective}

\textcolor{red}{(short paragraph introducing injective relations. Then list the order of explanations in this subsection)}

In general, a relation $R \subseteq X \times Y$ is {\em injective} if for all $x,x' \in X$ and $y \in Y$, $xRy$ and $x'Ry$ implies $x = x'$.
Equivalently, $R$ is injective if all the $R(x)$ for $x \in X$ are disjoint.

\begin{lemma}\label{lem:comInjective}
If both $R \subseteq X \times Y$ and $R' \subseteq Y \times Z$ are injective, then $RR' \subseteq X \times Z$ is injective.
\end{lemma}

\begin{proof}
If $xRR'z$ and $x'RR'z$ then there are $y,y' \in Y$ such that $xRy$ and $yR'z$, and $x'Ry'$ and $y'R'z$. By the injectivity of $R'$ we have $y = y'$,
hence $x = x'$ by the injectivity of $R$.
\end{proof}

We say that $(S, P)$ is {\em injective} if $|(S, P)| = S \cup P$ is such in the above sense.

\begin{lemma}\label{lem:biRelInjectiveComp}
If both $(S,P)$ and $(S',P')$ are injective, then $(S,P) \comp (S',P')$ is injective.
\end{lemma}

\begin{proof}
By (\ref{eq:unionComp}) in Section \ref{sec:size-change},  we know that $|(S,P) \comp (S',P')| =  |(S,P)| |(S',P')|$ which is ordinary composition of the injective reations $S \cup P$ and $S' \cup P'$, hence it is injective by Lemma \ref{lem:comInjective}.
\end{proof}

In case of injective bi-partite relations, composition can be simplified by:
\[ (S,P) \comp (S',P') = (SS', SP' \cup PS' \cup PP') \]
Indeed, by injectivity if $xSyS'z$ then $y \not \in \cod{P} \cup \dom{P'}$ hence $(x,z) \not \in ( SP' \cup PS' \cup PP')$.

\medskip
We say that the set of bi-partite relations $\biRel$ is {\em injective} if all $(S,P) \in \biRel$ is such. By Lemma \ref{lem:biRelInjectiveComp}, if $\biRel$
is injective then $\biRel^*$ is such. If $\Graph = (V, \biRel, E)$ with injective $\biRel$, by abuse of language we say that $\Graph$ is injective.

\textcolor{red}{(We argue here that in both $\CLKIDomega$ and $\CLJIDomega$ they have injective relations, then we formally prove it in the following theorem)}

\begin{theorem}
If $\Graph_\Pi$ is the transition graph associated with a cyclic proof $\Pi$ of $\CLKIDomega$, then $\Graph_\Pi$ is injective.
\end{theorem}

\begin{remark}
    \textcolor{red}{(Provide here the explanation on how to proceed/prove the same theorem with multisets instead of sets)}
\end{remark}


\input{CSL-FPC}
